export type DetailSection = {
  title: string;
  items: string[];
};

export type ListSection = {
  title: string;
  items: string[];
};

export type RelatedJob = {
  tag: string;
  title: string;
  location: string;
  type: string;
  highlighted?: boolean;
};
